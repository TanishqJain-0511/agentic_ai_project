fastapi
uvicorn
psycopg2-binary
sqlalchemy
asyncpg
python-dotenv
alembic # For database migrations
pgvector # For pgvector integration

python-jose[cryptography] # For JWT
passlib[bcrypt] # For password hashing
pydantic # For data validation
pydantic-settings # For environment variable management
redis # For Celery and caching
celery # For background tasks
langfuse # For observability
langchain # General LLM orchestration (if needed for agents)
langgraph # For agent orchestration (specifically compliance agent)
ragas # For evaluation
deepeval # For evaluation
sentence-transformers # For open cross-encoder and embeddings
numpy # For Monte Carlo simulation
scipy # For Monte Carlo simulation
pyyaml # For policy engine configuration
ollama # If using Python client for Ollama, otherwise interact via API
httpx # For making HTTP requests (e.g., to MFAPI.in or Ollama)
requests # Alternative to httpx
pandas # For data processing, e.g., historical returns
openpyxl # For reading Excel files, if historical data is in that format
